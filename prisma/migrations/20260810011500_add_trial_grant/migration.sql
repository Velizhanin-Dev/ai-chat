-- Отметка о выдаче пробного периода. Сравнивается с AppSetting "trial_reset_at":
-- если метка сброса новее — при первом заходе юзер получает пробный период заново
-- (персонально, от момента его визита, а не от момента нажатия кнопки в админке).
ALTER TABLE "User" ADD COLUMN "trialGrantedAt" TIMESTAMP(3);
