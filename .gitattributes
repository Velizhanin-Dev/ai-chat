# Шелл-скрипты всегда с LF, даже если репозиторий клонируют на Windows.
# ⚠️ Иначе bash на сервере падает на «\r»: строки заканчиваются CRLF, и он читает
# невидимый символ как часть команды («invalid option», «syntax error near…»).
*.sh text eol=lf
